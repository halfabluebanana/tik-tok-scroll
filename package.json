{
  "name": "tiktok-app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "nodemon server.js",
    "build": "cd client && npm run build",
    "watch": "npm run build && nodemon --watch src --ext js,jsx,css,html --exec \"npm run build\" --cwd client",
    "install-all": "npm install && cd client && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "proxy": "http://localhost:3001",
  "dependencies": {
    "@serialport/parser-readline": "^10.5.0",
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "multer": "^1.4.5-lts.1",
    "nodemon": "^2.0.19",
    "serialport": "^10.5.0",
    "ws": "^8.18.2"
  }
}
